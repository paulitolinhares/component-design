export function saveUser({ username, password }) {
  alert(`saved ${username} with the password ${password}`);
}

export function editUser({ username, password }) {
  alert(`edited ${username} to the password ${password}`);
}
